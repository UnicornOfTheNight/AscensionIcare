<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.superjump">

    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS" />

    <uses-feature
        android:name="android.hardware.sensor.accelerometer"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="Superjump"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AscensionIcare">

        <!-- GameOverActivity sans intent-filter -->
        <activity
            android:name=".GameOverActivity"
            android:exported="true" />

        <!-- Level4Activity avec intent-filter (point d’entrée) -->
        <activity
            android:name=".Level4Activity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Autres activités (ex. Level1, LevelEnd...) -->
        <activity
            android:name=".Level1Activity"
            android:exported="true" />

    </application>

</manifest>
